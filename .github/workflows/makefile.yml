name: Build Check

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# ── Dependency pins ──────────────────────────────────────────────────────
# The pin SHAs live in ONE manifest, .github/ci-pins.env, loaded into the job
# environment by the early "Load dependency pins" step (below) so this workflow
# and native-build.yml (Coverity) read the same source of truth. To bump a
# dependency, change the SHA in ci-pins.env AND the matching fallback in setup.sh
# (the "Verify pin manifest" step enforces the match — a mismatch errors).
# setup.sh fallbacks keep `make setup` working locally without the workflow;
# HAL CI clones OneWifi/develop and runs its setup.sh, so those fallbacks are the
# cross-repo truth. See ci-README.md §7.

# Stage 1 is deliberately read-only: it builds PR-controlled makefiles, so it
# must hold no write scope. The PR-comment posting (writes) happens only in the
# trusted workflow_run stage (pr-comments.yml).
permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04

    strategy:
      matrix:
        platform:
          - name: "Raspberry Pi"
            id: rpi                   # has to be stable for the ci-summary-<id> stage
            makefile_path: "build/linux/rpi/makefile"
            extra_coverage: "0"
            extra_cflags: ""
          - name: "Banana Pi R4 - MLO"
            id: bpi
            makefile_path: "build/linux/bpi/makefile"
            extra_coverage: "1"
            extra_cflags: "-DCONFIG_GENERIC_MLO"
            clang_tidy: true          # only this leg builds the compile DB + runs clang-tidy
          - name: "Platform Mock Unittests"
            id: mock
            makefile_path: "build/linux/mockplatform/makefile"
            extra_coverage: "0"
            extra_cflags: ""
      fail-fast: false

    name: Build for ${{ matrix.platform.name }}

    steps:
    - name: Checkout current repository
      uses: actions/checkout@v6
      with:
        path: 'OneWifi'
        fetch-depth: 0                # clang-tidy diffs changed source against the PR base
        persist-credentials: false    # nothing here pushes; don't leave the token in .git/config

    - name: Clone unified-wifi-mesh repository
      run: |
        mkdir -p easymesh_project
        git clone https://github.com/rdkcentral/unified-wifi-mesh.git easymesh_project/unified-wifi-mesh
        mv OneWifi easymesh_project/OneWifi

    - name: Load dependency pins
      run: |
        set -uo pipefail
        # ONE source of truth for every pinned dependency SHA (ci-pins.env),
        # loaded into $GITHUB_ENV so later steps read $PIN_* exactly as the old
        # env: block exposed them. native-build.yml loads the same file. The
        # strict PIN_NAME=<40hex> regex is the gate: a typo'd or truncated pin
        # must fail here, never load as a silent partial value.
        manifest=easymesh_project/OneWifi/.github/ci-pins.env
        [ -f "$manifest" ] || { echo "::error::pin manifest $manifest missing"; exit 1; }
        pins=$(grep -E '^PIN_[A-Z0-9_]+=[0-9a-f]{40}$' "$manifest" || true)
        # Real value lines = non-blank, non-comment (unpinned entries are commented
        # out). Every one of them must be a well-formed pin, else a SHA is malformed.
        real=$(grep -cE '^[[:space:]]*[^#[:space:]]' "$manifest" || true)
        matched=$(printf '%s\n' "$pins" | grep -c . || true)
        if [ -z "$pins" ] || [ "$matched" -ne "$real" ]; then
          echo "::error::pin manifest $manifest: $matched of $real value line(s) matched PIN_NAME=<40hex> — a pin is malformed or truncated"
          exit 1
        fi
        printf '%s\n' "$pins" >> "$GITHUB_ENV"
        echo "loaded $matched dependency pin(s) from $manifest"

    - name: Set up dependencies
      run: |
        sudo apt-get update
        # Pruned: removed mariadb-server (100MB, unused — OneWifi doesn't build the
        # mesh agent that links -lmariadb), golang (runner has Go in toolcache),
        # libmysqlcppconn-dev (0 includes/linkage), vim (CI doesn't need an editor).
        # --no-install-recommends avoids pulling unneeded transitive packages.
        # See ~/ci-aptCache.md for the full analysis.
        sudo apt-get install -y --no-install-recommends \
                                build-essential \
                                cmake \
                                python3 \
                                python3-pip \
                                git \
                                libev-dev \
                                libjansson-dev \
                                zlib1g-dev \
                                libnl-3-dev \
                                libnl-genl-3-dev \
                                libnl-route-3-dev \
                                libavro-dev \
                                libcjson1 libcjson-dev \
                                libprotobuf-c-dev \
                                libssl-dev \
                                uuid-dev \
                                libreadline-dev \
                                iptables \
                                gnupg \
                                file \
                                pkg-config \
                                libperl-dev \
                                libjson-c-dev \
                                libgtest-dev \
                                libgmock-dev \
                                bear \
                                clang-tidy-18

        # Steps for ucode installation required for hostapd compilation
        git clone https://github.com/jow-/ucode.git ucode
        cd ucode
        git checkout "$PIN_UCODE"
        # Assert the tree, not just the SHA: if this pin ever lands on a commit
        # that defines the `unused` macro, fail here instead of deep in the HAL build.
        if git grep -q 'define unused' HEAD -- include/; then
          echo "::error::ucode pin $PIN_UCODE leaks the 'unused' macro (collides with OpenSSL). Check the pin — see the TRAP note in the manifest."
          exit 1
        fi
        mkdir build && cd build
        cmake -DUBUS_SUPPORT=OFF -DUCI_SUPPORT=OFF -DULOOP_SUPPORT=OFF ..
        make -j"$(nproc)"
        sudo make install
        sudo ldconfig

    # Guard: the manifest pins (ci-pins.env, loaded above) MUST match the setup.sh
    # fallbacks. A mismatch means someone bumped only the manifest → the cache key
    # (which hashes setup.sh) won't invalidate → CI silently builds the old pinned
    # tree. HAL CI runs setup.sh directly (no manifest env vars), so fallback drift
    # also means cross-repo divergence. Fail loud at setup time, not deep in a build.
    - name: Verify pin manifest matches setup.sh fallbacks
      if: matrix.platform.id != 'mock'
      working-directory: easymesh_project/OneWifi
      run: |
        set -uo pipefail
        # The pins must have loaded ("Load dependency pins"). An empty value here
        # means the manifest failed to load — fail before the grep checks below
        # silently pass on an empty needle. All three pins are named symmetrically
        # so the SC2153 "possible misspelling" heuristic can't single one out (they
        # are assigned via $GITHUB_ENV, which the linter cannot see statically).
        if [ -z "${PIN_HOSTAP_2_11:-}" ] || [ -z "${PIN_HOSTAP_2_10:-}" ] || [ -z "${PIN_META_FILOGIC:-}" ]; then
          echo "::error::a dependency pin is empty — the pin manifest did not load."
          exit 1
        fi
        ok=true
        check() { # usage: check ENV_NAME SETUP_FILE
          local manifest="$1" file="$2" name="$3"
          if ! grep -qF "$manifest" "$file"; then
            echo "::error::Pin mismatch: $name manifest='$manifest' not found in $file"
            ok=false
          fi
        }
        if [ "${{ matrix.platform.id }}" = "bpi" ]; then
          check "$PIN_HOSTAP_2_11"  build/linux/bpi/setup.sh PIN_HOSTAP_2_11
          check "$PIN_META_FILOGIC" build/linux/bpi/setup.sh PIN_META_FILOGIC
        else
          check "$PIN_HOSTAP_2_10"  build/linux/rpi/setup.sh PIN_HOSTAP_2_10
        fi
        # native-build.yml (Coverity) must read its hostap pin from the manifest,
        # never re-hardcode a SHA — a third copy would drift silently. Assert no
        # bare 40-hex literal has crept back into it.
        if grep -nE '[0-9a-f]{40}' .github/workflows/native-build.yml; then
          echo "::error::native-build.yml carries a 40-hex literal — it must load pins from ci-pins.env, not hardcode a SHA."
          ok=false
        fi
        $ok || exit 1

    # Perf: each device leg (bpi, rpi) clones + patches hostap from upstream.
    # The patched tree is a pure function of the pins in setup.sh plus the patch
    # inputs, so the hostap-tree composite action caches it keyed on those files
    # (patched-<leg> mode). On a hit, setup.sh's .hostap_patched flag skips the
    # clone AND patching entirely; a miss falls back to the normal clone, so this
    # can never be slower than today. The key logic (files hash + the one unpinned
    # patch source per leg: bpi's meta-cmf-bananapi kernel_6_6 tree SHA, rpi's
    # hostap-patches tip) lives in the action so Coverity (native-build.yml, plain
    # mode) shares the same cache machinery. mock never clones hostap.
    #
    # NOTE: the local `uses:` path is ./easymesh_project/OneWifi/… NOT ./.github/… —
    # the checkout lands at OneWifi/ then is mv'd under easymesh_project/OneWifi
    # (above), so the action only exists at that sub-path (ci-README §10).
    - name: Provide patched hostap tree (cached)
      if: matrix.platform.id != 'mock'
      uses: ./easymesh_project/OneWifi/.github/actions/hostap-tree
      with:
        mode: patched-${{ matrix.platform.id }}
        path: easymesh_project/rdk-wifi-libhostap
        github-token: ${{ github.token }}

    - name: Setup OneWiFi for ${{ matrix.platform.name }}
      working-directory: easymesh_project/OneWifi
      run: |
        # $GITHUB_ACTOR is a built-in runner env var; use it rather than
        # interpolating ${{ github.actor }} straight into the script.
        git config --global user.email "${GITHUB_ACTOR}@users.noreply.github.com"
        git config --global user.name "${GITHUB_ACTOR}"
        make -f ${{ matrix.platform.makefile_path }} setup

    - name: Build OneWiFi for ${{ matrix.platform.name }}
      id: build
      working-directory: easymesh_project/OneWifi
      run: |
        # pipefail so make's exit status (not tee's) decides the step result.
        set -o pipefail
        # clang-tidy leg only: wrap make in `bear` so ONE build yields the objects
        # AND compile_commands.json (no second build). Other legs run make plain.
        ${{ matrix.platform.clang_tidy && 'bear --output compile_commands.json --' || '' }} make -f ${{ matrix.platform.makefile_path }} -j"$(nproc)" all 2>&1 | tee build.log
      env:
        # Coverage tier for the bpi target: "1" enables EXTRA_COV_FLAGS
        # (ONEWIFI_DB_SUPPORT + memwraptool). Ignored by rpi/mockplatform.
        EXTRA_COVERAGE: ${{ matrix.platform.extra_coverage }}
        # Extra defines appended to CFLAGS. bpi appends via 'CFLAGS +=' (so the
        # MLO build picks up -DCONFIG_GENERIC_MLO); rpi/mockplatform use
        # 'CFLAGS =' and ignore it. -j is safe: PROGRAM depends on libwifihal.a.
        CFLAGS: ${{ matrix.platform.extra_cflags }}

    - name: Build summary for ${{ matrix.platform.name }}
      if: always()   # run on failure too, so the red-build reason is surfaced
      working-directory: easymesh_project/OneWifi
      run: |
        # ci-out/ collects the stage-1 artifacts the PR-comment stage posts.
        mkdir -p ci-out
        LOG=build.log
        if [ ! -f "$LOG" ]; then
          echo "### ${{ matrix.platform.name }} — no build log captured" \
            | tee ci-out/build-summary.md >> "$GITHUB_STEP_SUMMARY"
          exit 0
        fi
        # Compiler + linker + make errors: what usually explains a red build,
        errs=$(grep -hE ': (error|fatal error):|undefined reference to|Error [0-9]+' "$LOG" \
               | sed -E 's#[^ ]*/(OneWifi|rdk-wifi-hal|rdk-wifi-libhostap)/#\1/#; s#//#/#g' \
               | sort -u || true)
        # Persist the normalized error list for the red-build attribution step (5b).
        printf '%s\n' "$errs" > ci-out/build-errors.txt
        # Surface the top errors as ANNOTATIONS (the box at the top of the failed
        # check, which people actually check) — not only the run-summary page.
        if [ -n "$errs" ]; then
          printf '%s\n' "$errs" | head -10 | sed 's/%/%25/g; s/\r/%0D/g' \
            | while IFS= read -r l; do echo "::error::$l"; done
        fi
        {
          echo "### ${{ matrix.platform.name }} — build ${{ steps.build.outcome }}"
          if [ -n "$errs" ]; then
            echo "#### ❌ Errors ($(printf '%s\n' "$errs" | wc -l) unique)"
            echo '```'
            printf '%s\n' "$errs" | head -100
            echo '```'
          fi
          # Warnings from OneWifi's own sources ONLY, with location.
          # HAL/hostapd/mesh errors still break the build, so will be shown too.
          # OneWifi runs a Yocto-style clean baseline, so anything here
          # is a newly-surfaced bug to triage.
          # The leading 'easymesh_project/' gate keeps only in-tree sources; without it the
          # sibling-exclusion below would still admit system/toolchain warnings (/usr/include,
          # /usr/lib/gcc). Mirrors rdk-wifi-hal's makefile.yml so the two stay in sync.
          warns=$(grep -hE ': warning:' "$LOG" \
                  | grep -E 'easymesh_project/' \
                  | grep -vE 'rdk-wifi-hal|rdk-wifi-libhostap|unified-wifi-mesh|halinterface|trower-base64' \
                  | sed -E 's#[^ ]*/OneWifi/+##; s#//#/#g' \
                  | sort -u || true)
          if [ -n "$warns" ]; then
            echo "#### ⚠️ OneWifi warnings ($(printf '%s\n' "$warns" | wc -l))"
            echo '```'
            printf '%s\n' "$warns" | head -100
            echo '```'
          else
            echo "#### ⚠️ OneWifi warnings: 0"
          fi
        } | tee ci-out/build-summary.md >> "$GITHUB_STEP_SUMMARY"

    - name: clang-tidy (gate + advisory) for ${{ matrix.platform.name }}
      if: always() && matrix.platform.clang_tidy
      working-directory: easymesh_project/OneWifi
      run: |
        # Two-tier clang-tidy over PR-changed .c/.cpp files (see .clang-tidy):
        #   * WarningsAsErrors checks    -> `error:`   -> FATAL (this step fails the job)
        #   * rest of enabled bugprone-* -> `warning:` -> advisory only
        # clang-diagnostic-* (most false positives and most noisy) is filtered out so it can NEVER
        # gate. The gate is changed-LINES-scoped: a PR can only fail on a .c/.cpp LINE it touched
        # (the per-file `lines=` filter below), so pre-existing findings on untouched lines never red it.
        mkdir -p ci-out
        if [ ! -f compile_commands.json ]; then
          echo "#### 🔎 clang-tidy: no compile DB" >> "$GITHUB_STEP_SUMMARY"
          # skipped (not clean) so the stage-2 poster leaves inline comments alone.
          printf '%s' '{"source":"clang-tidy","status":"skipped","dropped":0,"comments":[]}' > ci-out/inline-tidy.json
          exit 0
        fi
        BASE='${{ github.event.pull_request.base.sha }}'
        CHANGED=''
        DIFF_BASE="$BASE"
        if [ -n "$BASE" ]; then
          git fetch --no-tags --depth=1 origin "$BASE" >/dev/null 2>&1 || true
          # Diff against the merge ref's real base parent (HEAD^1), not the frozen payload
          # BASE: this step's checkout has no `ref:`, so HEAD is refs/pull/N/merge and HEAD^1
          # is the CURRENT base. On a RE-RUN of an old run the merge ref re-resolves to today's
          # base while $BASE stays pinned, so a two-dot $BASE..HEAD would blame post-fork
          # base-branch changes on the PR (gate/line-scope fires on lines the author never
          # touched). Use HEAD^1 when HEAD is a merge commit AND $BASE is its ancestor (the
          # is-ancestor probe makes the parent-order assumption self-verifying); else keep
          # $BASE. Mirrors gcc_diff_gate.py effective_base().
          if git rev-parse --verify --quiet HEAD^2 >/dev/null \
             && git merge-base --is-ancestor "$BASE" HEAD^1 2>/dev/null; then
            DIFF_BASE=HEAD^1
          fi
          # Changed file not present in compile_commands.json (not built) falls back to default
          # flags. A real source then can't resolve its includes -> a fatal clang-diagnostic
          # aborts file parsing before any check run on it -> that output is dropped by the
          # clang-diagnostic filter below -> file can't fail. Thus, only built files are judged.
          # Exclude only the vendored hostap tree (sibling clone rdk-wifi-libhostap/), never
          # first-party sources: '^build/' wrongly dropped build/linux/compat/coverage_stubs.c
          # (compiled by the makefile, real DB entry) and bare 'hostap' dropped e.g.
          # wifi_hostapd_glue.c. A changed file not in the DB self-skips (default-flags fallback
          # -> fatal clang-diagnostic -> filtered below), so DB membership is the real gate.
          # Anchored '^': git diff paths are repo-relative and the vendored tree, if ever
          # surfaced, sits at the repo root -- so a first-party path that merely *contains*
          # the name (e.g. source/.../rdk-wifi-libhostap-notes.c) is never dropped.
          CHANGED=$(git diff --name-only --diff-filter=ACM "$DIFF_BASE" HEAD -- '*.c' '*.cpp' 2>/dev/null \
                    | grep -vE '^rdk-wifi-libhostap/' || true)
        fi
        # Extra checks this repo wants on top of the shared .clang-tidy config.
        # --warnings-as-errors COMBINES with the config's WAE (proven clang-tidy-18);
        # --checks APPENDS to the config's Checks. Both auto-inherit changed-lines
        # scope because the awk filter below runs on output, not input.
        # See clang-tidy-hal-extra-mechanism memory for the proof + HAL usage.
        EXTRA_WAE=''       # comma-separated bugprone checks to gate (empty = baseline only)
        EXTRA_CHECKS=''    # checks to ENABLE that .clang-tidy doesn't (e.g. clang-analyzer-*)
        extra=()
        [ -n "$EXTRA_WAE" ]    && extra+=(--warnings-as-errors="$EXTRA_WAE")
        [ -n "$EXTRA_CHECKS" ] && extra+=(--checks="$EXTRA_CHECKS")
        : > tidy.log
        for f in $CHANGED; do
          [ -f "$f" ] || continue
          # new-side line numbers this PR changed in *this* file (zero-context hunks)
          lines=$(git diff -U0 --diff-filter=ACM "$DIFF_BASE" HEAD -- "$f" 2>/dev/null \
                  | awk '/^@@ /{c=$3;sub(/^\+/,"",c);n=1;if(c~/,/){split(c,a,",");c=a[1];n=a[2]}
                         for(i=0;i<n;i++)if(n>0)print c+i}' | sort -un | paste -sd' ' -)
          [ -n "$lines" ] || continue
          # || true: a no-match grep (exit 1) or a clang-tidy hiccup must not abort the loop.
          # clang-tidy runs one file at a time -> every finding is this file's, so keep only the
          # ones whose LINE the PR changed. Filter on the line number alone: matching the
          # sed-normalized display path against git-diff paths is fragile (silently drops all).
          clang-tidy-18 -p . "${extra[@]}" --quiet "$f" 2>/dev/null \
            | grep -E ': (warning|error):' | grep -v clang-diagnostic \
            | awk -F: -v L="$lines" 'BEGIN{split(L,a," ");for(i in a)k[a[i]]} ($2 in k)' >> tidy.log || true
        done
        # WarningsAsErrors-promoted checks print as `error:`; everything else as `warning:`.
        gated=$(grep -E ': error:'   tidy.log | sed -E 's#[^ ]*/OneWifi/+##; s#//#/#g' | sort -u || true)
        advis=$(grep -E ': warning:' tidy.log | sed -E 's#[^ ]*/OneWifi/+##; s#//#/#g' | sort -u || true)
        if [ -n "$gated" ]; then
          printf '%s\n' "$gated" | head -10 | sed 's/%/%25/g; s/\r/%0D/g' \
            | while IFS= read -r l; do echo "::error::$l"; done
        fi
        if [ -n "$advis" ]; then
          printf '%s\n' "$advis" | head -10 | sed 's/%/%25/g; s/\r/%0D/g' \
            | while IFS= read -r l; do echo "::warning::$l"; done
        fi
        {
          if   [ -z "$CHANGED" ];     then echo "#### 🔎 clang-tidy: no changed .c/.cpp to scan"
          elif [ -z "$gated$advis" ]; then echo "#### 🔎 clang-tidy: clean on changed lines"
          else
            if [ -n "$gated" ]; then
              echo "#### ❌ clang-tidy gate — $(printf '%s\n' "$gated" | wc -l) fatal"
              echo '```'; printf '%s\n' "$gated" | head -100; echo '```'
              echo "_Fix the finding, or if it is a deliberate, waive it inline with \`// NOLINT(<check-name>)\` on that line._"
            fi
            if [ -n "$advis" ]; then
              echo "#### 🔎 clang-tidy advisory — $(printf '%s\n' "$advis" | wc -l) findings"
              echo '```'; printf '%s\n' "$advis" | head -100; echo '```'
            fi
          fi
        } | tee ci-out/tidy-summary.md >> "$GITHUB_STEP_SUMMARY"
        # Inline-review candidates (Commit 5): parse tidy.log (already changed-lines
        # scoped) into per-finding comments for the stage-2 'inline' job. Written
        # even when the gate fires below — findings post regardless of pass/fail.
        # Never reds the step (|| true): a parse hiccup must not fail the build.
        python3 .github/scripts/tidy_to_inline.py tidy.log ci-out/inline-tidy.json || true
        # Gate AFTER the summary + artifact are written, on promoted checks only.
        if [ -n "$gated" ]; then
          echo "clang-tidy gate: $(printf '%s\n' "$gated" | wc -l) promoted-check violation(s) on changed lines" >&2
          exit 1
        fi

    - name: gcc diff-scoped warning gate for ${{ matrix.platform.name }}
      # PROPOSAL (branch ci/gcc-diff-gate). Gate not-yet-promoted gcc classes on the
      # PR's changed LINES only, without promoting them tree-wide (the tree still carries a
      # backlog: -Wvla 18, -Wreturn-type 11, dead-code family). The script recompiles each changed
      # .c/.cpp from compile_commands.json with the candidate warnings enabled (non-fatal) and keeps
      # only findings on lines the PR touched -> a PR can only fail on a class that fires on a changed line.
      # The silent baseline (and the build-summary that relies on it) is unaffected. bpi leg only
      # (it owns the compile DB). Runs AFTER clang-tidy so both gate independently.
      if: always() && matrix.platform.clang_tidy && github.event_name == 'pull_request'
      working-directory: easymesh_project/OneWifi
      env:
        BASE: ${{ github.event.pull_request.base.sha }}
        # ROLLOUT: start in advisory mode. ENFORCE=false renders the ❌ "would fail"
        # block but never reds the job, so the mechanism runs on real PRs first.
        # FLIP to 'true' (one-word change) once ALL of these hold on the live runner:
        #   1. >= a handful of real PRs touching .c/.cpp have processed end-to-end
        #      with NO "mechanism error" summary line (the try/except fail-open);
        #   2. >= 1 advisory finding (the -Wunused-* classes fire on ordinary
        #      refactors) hand-checked against the diff -> line attribution correct;
        #   3. zero false positives on the GATE classes.
        # Gate on that evidence, not the calendar. Silence of the GATE classes alone
        # proves nothing (they're rarely introduced) -- the advisory classes are what
        # exercise the recompile + line-scoping machinery.
        ENFORCE: 'false'
        # GATE = fail the job (when ENFORCE=true) if introduced on a changed line.
        # ADVISORY = report only. -Wvla (stack-overflow) + -Wreturn-type (UB) are
        # high-impact + reliable -> first to gate. The dead-code family is
        # diff-scope-reliable but kept ADVISORY (promote a class into GATE_WARNINGS
        # once quiet on real PRs). (-Wunreachable-code-* are clang-only; the "moved
        # code left set-but-unused vars" case is -Wunused-but-set-variable.)
        GATE_WARNINGS: "-Wvla -Wreturn-type"
        ADVISORY_WARNINGS: "-Wunused-but-set-variable -Wunused-value -Wunused-label"
        # Inline-review candidates (Commit 5) for the stage-2 'inline' job. Lands in
        # ci-out/ (uploaded as ci-summary-bpi). Absent/unset -> the script writes none.
        INLINE_JSON: "ci-out/inline-gcc.json"
      run: |
        set -uo pipefail
        mkdir -p ci-out
        if [ ! -f compile_commands.json ] || [ -z "$BASE" ]; then
          echo "#### 🚦 gcc diff-gate: no compile DB or PR base — skipped" >> "$GITHUB_STEP_SUMMARY"
          exit 0
        fi
        git fetch --no-tags --depth=1 origin "$BASE" >/dev/null 2>&1 || true
        # The script prints a markdown summary to stdout and ::error::/::warning:: to stderr;
        # it exits 1 iff a GATE class fired on a changed line.
        # A missing script (partial cherry-pick / not yet on this branch) must skip,
        # not red the job: python3 on an absent file exits 2 and would fail the matrix.
        if [ ! -f .github/scripts/gcc_diff_gate.py ]; then
          echo "#### 🚦 gcc diff-gate: script not present — skipped" | tee -a ci-out/gcc-gate-summary.md >> "$GITHUB_STEP_SUMMARY"
          exit 0
        fi
        set +e
        python3 .github/scripts/gcc_diff_gate.py > gcc-gate.md
        rc=$?
        set -e
        tee -a ci-out/gcc-gate-summary.md < gcc-gate.md >> "$GITHUB_STEP_SUMMARY"
        exit "$rc"

    # Red-build attribution (Commit 5b): only when THIS leg's build step failed.
    # Placed LAST among the analysis steps — it reversibly reverts the PR to test
    # whether the failing TUs compile on base sources, so it must run after
    # clang-tidy + the gcc gate (which need the pristine PR tree). Advisory and
    # fail-open: the script always exits 0 and always restores the tree. Tier 2
    # (the definitive revert-and-recompile verdict) runs only where a compile DB
    # exists (HAVE_DB, i.e. the bpi leg); other legs emit the tier-1 heuristic.
    - name: Red-build attribution for ${{ matrix.platform.name }}
      if: always() && steps.build.outcome == 'failure' && github.event_name == 'pull_request' && matrix.platform.id != 'mock'
      working-directory: easymesh_project/OneWifi
      env:
        BASE: ${{ github.event.pull_request.base.sha }}
        # 'true' only on a leg that built compile_commands.json (bpi runs bear).
        # Gate tier 2 on this, NEVER on a bare file check: a stale DB on a DB-less
        # leg would recompile with the wrong flags and produce a confident wrong verdict.
        HAVE_DB: ${{ matrix.platform.clang_tidy && 'true' || '' }}
      run: |
        set -uo pipefail
        mkdir -p ci-out
        # A missing script (partial cherry-pick) must skip, not red the matrix.
        if [ ! -f .github/scripts/build_attribution.py ]; then
          echo "#### 🧭 red-build attribution: script not present — skipped" >> "$GITHUB_STEP_SUMMARY"
          exit 0
        fi
        git fetch --no-tags --depth=1 origin "$BASE" >/dev/null 2>&1 || true
        # The script is fail-open (always exits 0); || true guards a python launch failure.
        python3 .github/scripts/build_attribution.py >> "$GITHUB_STEP_SUMMARY" || true

    # ---- Hand-off to the trusted PR-comment stage (pr-comments.yml) ----------
    # Record the PR identity as passive data for the workflow_run poster to
    # validate. Only meaningful on PRs; push builds skip it.
    - name: Record PR metadata for the comment stage
      if: always() && github.event_name == 'pull_request'
      working-directory: easymesh_project/OneWifi
      run: |
        mkdir -p ci-out
        {
          echo "pr_number=${{ github.event.pull_request.number }}"
          echo "head_sha=${{ github.event.pull_request.head.sha }}"
        } > ci-out/pr-meta.env

    # Publish the summary artifact the PR-comment stage posts. Uploaded even on build failure
    # (if: always()) so a red build still gets its summary posted. tidy-summary.md for now
    # exists only on the bananapi build leg.
    #
    # Scoped to the two device legs (bpi + rpi), NOT mock. Both devices run the Yocto-style
    # clean baseline (-Wall -Wextra + explicit -Wno-), so their build-summaries are silent
    # *UNLESS* a genuinely new warning *class* appears.
    # NOTE: the build-summary greps the whole list of OneWifi warnings — it is NOT PR-diff-scoped
    # (unlike the clang-tidy step). It relies on that silent baseline, so any un-suppressed
    # residual warning would show on every PR. mock is the unittest leg (no clean-baseline gate)
    # -> excluded to avoid exactly that noise.
    # The pr-comments.yml 'build' job aggregates however many ci-summary-* exist
    # (and anchors trust on ci-summary-bpi, so bpi must always upload), so:
    #   TO ADD mock:     drop the `&& matrix.platform.id != 'mock'` clause below.
    #   TO REVERT to bpi-only: change it back to `== 'bpi'`.
    #   (nothing to change in pr-comments.yml either way.)
    - name: Upload CI summary artifact
      if: always() && github.event_name == 'pull_request' && matrix.platform.id != 'mock'
      uses: actions/upload-artifact@v6
      with:
        name: ci-summary-${{ matrix.platform.id }}
        path: easymesh_project/OneWifi/ci-out
        if-no-files-found: warn
        retention-days: 1
